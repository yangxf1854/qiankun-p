import styles from './index.less';

const IndexPage = () => {
  console.log(222);
  
  return (
    <div>
      <h1 className={styles.title}>parent index</h1>
    </div>
  );
}

export default IndexPage
